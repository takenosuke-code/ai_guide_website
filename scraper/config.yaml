wordpress:
  # Your WordPress site URL (no trailing slash)
  url: "http://aitoolsite1020-vqchs.wordpress.com"
  
  # WordPress username
  username: "ai.tool.site1020@gmail.com"
  
  # Application Password (24 characters from WordPress)
  # Get this from: Users → Your Profile → Application Passwords
  password: "a6ZcYEQHH9G5u2YoEATP17bC"
  
  # Category slug for AI tools (your "ai-review" category)
  category_slug: "ai-review"

scraper:
  # User agent for HTTP requests (FREE - just identifies your scraper to websites)
  # This is just a text string, no service needed. Websites use it to identify browsers/scrapers
  # You can leave this as-is or change it to any text you want
  user_agent: "Mozilla/5.0 (compatible; AI-Scraper/1.0)"
  
  # Request timeout in seconds
  timeout: 10
  
  # Minimum hours between scrapes (even if frequency is lower)
  min_hours_between_scrapes: 1

